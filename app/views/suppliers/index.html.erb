<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Suppliers Management - HKMS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 2rem 0;
            color: white;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
        }
        
        .main-container {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 2rem;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            animation: fadeInUp 0.8s ease-out;
        }
        
        .page-title {
            text-align: center;
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 2rem;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }
        
        .action-buttons {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
        }
        
        .btn {
            padding: 0.75rem 1.5rem;
            border-radius: 8px;
            font-weight: 600;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }
        
        .btn-primary {
            background: linear-gradient(45deg, #007bff, #0056b3);
            border: none;
            color: white;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 123, 255, 0.4);
            color: white;
        }
        
        .btn-secondary {
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            color: white;
        }
        
        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.3);
            color: white;
            transform: translateY(-2px);
        }
        
        .btn-warning {
            background: linear-gradient(45deg, #ffc107, #e0a800);
            border: none;
            color: #212529;
        }
        
        .btn-danger {
            background: linear-gradient(45deg, #dc3545, #c82333);
            border: none;
            color: white;
        }
        
        .suppliers-table {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            overflow: hidden;
            backdrop-filter: blur(5px);
        }
        
        .suppliers-table th {
            background: rgba(255, 255, 255, 0.2);
            color: white;
            font-weight: 600;
            padding: 1rem;
            border: none;
        }
        
        .suppliers-table td {
            padding: 1rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: rgba(255, 255, 255, 0.9);
        }
        
        .suppliers-table tbody tr:hover {
            background: rgba(255, 255, 255, 0.1);
        }
        
        .action-buttons-cell {
            display: flex;
            gap: 0.5rem;
        }
        
        .action-buttons-cell .btn {
            padding: 0.4rem 0.8rem;
            font-size: 0.875rem;
        }
        
        .alert {
            background: rgba(220, 53, 69, 0.1);
            border: 1px solid rgba(220, 53, 69, 0.3);
            border-radius: 8px;
            color: #fff;
            margin-bottom: 1.5rem;
            padding: 1rem;
        }
        
        .alert-success {
            background: rgba(40, 167, 69, 0.1);
            border-color: rgba(40, 167, 69, 0.3);
        }
        
        .no-suppliers {
            text-align: center;
            padding: 3rem;
            color: rgba(255, 255, 255, 0.8);
            font-size: 1.1rem;
        }
        
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @media (max-width: 768px) {
            .container {
                padding: 0 0.5rem;
            }
            
            .main-container {
                padding: 1rem;
            }
            
            .page-title {
                font-size: 2rem;
            }
            
            .action-buttons {
                flex-direction: column;
                gap: 1rem;
            }
            
            .suppliers-table {
                font-size: 0.875rem;
            }
            
            .action-buttons-cell {
                flex-direction: column;
                gap: 0.25rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="main-container">
            <h1 class="page-title">
                <i class="fas fa-truck"></i> Suppliers Management
            </h1>
            
            <% if notice %>
                <div class="alert alert-success">
                    <i class="fas fa-check-circle"></i> <%= notice %>
                </div>
            <% end %>
            
            <% if alert %>
                <div class="alert">
                    <i class="fas fa-exclamation-triangle"></i> <%= alert %>
                </div>
            <% end %>
            
            <div class="action-buttons">
                <%= link_to storekeeper_index_path, class: "btn btn-secondary" do %>
                    <i class="fas fa-arrow-left"></i> Back to Storekeeper
                <% end %>
                
                <%= link_to new_supplier_path, class: "btn btn-primary" do %>
                    <i class="fas fa-plus"></i> Add New Supplier
                <% end %>
            </div>
            
            <% if @suppliers.any? %>
                <div class="table-responsive">
                    <table class="table suppliers-table">
                        <thead>
                            <tr>
                                <th>Supplier Code</th>
                                <th>Name</th>
                                <th>Phone</th>
                                <th>Email</th>
                                <th>Address</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% @suppliers.each do |supplier| %>
                                <tr>
                                    <td><strong><%= supplier.supplier_code %></strong></td>
                                    <td><%= supplier.name %></td>
                                    <td><%= supplier.phone_number %></td>
                                    <td><%= supplier.email %></td>
                                    <td><%= supplier.address %></td>
                                    <td>
                                        <div class="action-buttons-cell">
                                            <%= link_to supplier_path(supplier), class: "btn btn-info btn-sm" do %>
                                                <i class="fas fa-eye"></i>
                                            <% end %>
                                            <%= link_to edit_supplier_path(supplier), class: "btn btn-warning btn-sm" do %>
                                                <i class="fas fa-edit"></i>
                                            <% end %>
                                            <%= link_to supplier_path(supplier), method: :delete, 
                                                confirm: "Are you sure you want to delete this supplier?", 
                                                class: "btn btn-danger btn-sm" do %>
                                                <i class="fas fa-trash"></i>
                                            <% end %>
                                        </div>
                                    </td>
                                </tr>
                            <% end %>
                        </tbody>
                    </table>
                </div>
            <% else %>
                <div class="no-suppliers">
                    <i class="fas fa-box-open fa-3x mb-3"></i>
                    <p>No suppliers found. Add your first supplier to get started!</p>
                </div>
            <% end %>
        </div>
    </div>
</body>
</html>