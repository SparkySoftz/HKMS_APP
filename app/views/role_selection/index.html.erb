<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select Your Role - HKMS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
        }
        
        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 2rem;
        }
        
        .header {
            text-align: center;
            margin-bottom: 3rem;
            animation: fadeInDown 1s ease-out;
        }
        
        .header h1 {
            font-size: 3rem;
            font-weight: bold;
            margin-bottom: 0.5rem;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }
        
        .header p {
            font-size: 1.3rem;
            opacity: 0.9;
            margin: 0;
        }
        
        .roles-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }
        
        .role-card {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 2rem;
            text-align: center;
            transition: all 0.3s ease;
            border: 2px solid transparent;
            backdrop-filter: blur(10px);
            animation: fadeInUp 1s ease-out;
            cursor: pointer;
        }
        
        .role-card:hover {
            transform: translateY(-5px);
            background: rgba(255, 255, 255, 0.2);
            border-color: rgba(255, 255, 255, 0.3);
            box-shadow: 0 15px 35px rgba(0,0,0,0.2);
        }
        
        .role-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            display: block;
        }
        
        .role-title {
            font-size: 1.5rem;
            font-weight: bold;
            margin-bottom: 0.5rem;
        }
        
        .role-description {
            font-size: 1rem;
            opacity: 0.8;
            margin: 0;
        }
        
        /* Role-specific colors */
        .admin-card { border-left: 5px solid #ff6b6b; }
        .customer-card { border-left: 5px solid #4ecdc4; }
        .chef-card { border-left: 5px solid #ffe66d; }
        .accountant-card { border-left: 5px solid #a8e6cf; }
        .storekeeper-card { border-left: 5px solid #ff8b94; }
        .cashier-card { border-left: 5px solid #c7ceea; }
        .direct-order-card { border-left: 5px solid #ff9a8b; }
        
        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        /* Animation delays for staggered effect */
        .role-card:nth-child(1) { animation-delay: 0.1s; }
        .role-card:nth-child(2) { animation-delay: 0.2s; }
        .role-card:nth-child(3) { animation-delay: 0.3s; }
        .role-card:nth-child(4) { animation-delay: 0.4s; }
        .role-card:nth-child(5) { animation-delay: 0.5s; }
        .role-card:nth-child(6) { animation-delay: 0.6s; }
        .role-card:nth-child(7) { animation-delay: 0.7s; }
        
        @media (max-width: 768px) {
            .header h1 {
                font-size: 2.5rem;
            }
            
            .header p {
                font-size: 1.1rem;
            }
            
            .roles-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }
            
            .role-card {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Welcome to HKMS</h1>
            <p>Please select your role to continue</p>
        </div>
        
        <div class="roles-grid">
            <div class="role-card admin-card" onclick="selectRole('admin')">
                <i class="fas fa-user-shield role-icon"></i>
                <div class="role-title">Admin</div>
                <div class="role-description">System administration and management</div>
            </div>
            
            <div class="role-card customer-card" onclick="selectRole('customer')">
                <i class="fas fa-user role-icon"></i>
                <div class="role-title">Customer</div>
                <div class="role-description">Book rooms and access services</div>
            </div>
            
            <div class="role-card chef-card" onclick="selectRole('chef')">
                <i class="fas fa-utensils role-icon"></i>
                <div class="role-title">Chef</div>
                <div class="role-description">Manage kitchen and food orders</div>
            </div>
            
            <div class="role-card accountant-card" onclick="selectRole('manager')">
                <i class="fas fa-user-tie role-icon"></i>
                <div class="role-title">Manager</div>
                <div class="role-description">Menu management and inventory oversight</div>
            </div>
            
            <div class="role-card storekeeper-card" onclick="selectRole('store_keeper')">
                <i class="fas fa-boxes role-icon"></i>
                <div class="role-title">Store Keeper</div>
                <div class="role-description">Inventory and supply management</div>
            </div>
            
            <div class="role-card cashier-card" onclick="selectRole('cashier')">
                <i class="fas fa-cash-register role-icon"></i>
                <div class="role-title">Cashier</div>
                <div class="role-description">Process payments and transactions</div>
            </div>
            
            <div class="role-card direct-order-card" onclick="selectRole('direct_order')">
                <i class="fas fa-shopping-cart role-icon"></i>
                <div class="role-title">Place an Order</div>
                <div class="role-description">Place an order without registration</div>
            </div>
        </div>
    </div>
    
    <script>
        function selectRole(role) {
            // Add a visual feedback
            const card = event.currentTarget;
            card.style.transform = 'scale(0.95)';
            
            setTimeout(() => {
                if (role === 'admin') {
                    // Redirect to admin login for authentication
                    window.location.href = '/admin/login';
                } else if (role === 'store_keeper') {
                    // Redirect to storekeeper dashboard
                    window.location.href = '/storekeeper';
                } else if (role === 'manager') {
                    // Redirect to manager dashboard for menu management
                    window.location.href = '/manager/dashboard';
                } else if (role === 'cashier') {
                    // Redirect to cashier dashboard
                    window.location.href = '/cashier/dashboard';
                } else if (role === 'customer') {
                    // Redirect to customer table selection
                    window.location.href = '/customer/table_selection';
                } else if (role === 'direct_order') {
                    // Redirect to direct order placement
                    window.location.href = '/orders/new';
                } else {
                    // For other roles, show alert for now
                    alert(`Selected role: ${role.replace('_', ' ').toUpperCase()}\n\nThis will be implemented soon!`);
                    
                    // Future implementation: redirect to role-specific dashboards
                    // window.location.href = `/${role}/dashboard`;
                }
            }, 150);
        }
        
        // Add entrance animation
        document.addEventListener('DOMContentLoaded', function() {
            const cards = document.querySelectorAll('.role-card');
            cards.forEach(card => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(30px)';
            });
            
            setTimeout(() => {
                cards.forEach((card, index) => {
                    setTimeout(() => {
                        card.style.opacity = '1';
                        card.style.transform = 'translateY(0)';
                        card.style.transition = 'all 0.6s ease';
                    }, index * 100);
                });
            }, 300);
        });
    </script>
</body>
</html>