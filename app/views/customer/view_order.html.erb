<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Order - HKMS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 2rem 1rem;
            color: white;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
        }
        
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            animation: fadeInDown 1s ease-out;
        }
        
        .header-title {
            font-size: 2rem;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }
        
        .table-info {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            padding: 0.5rem 1rem;
            backdrop-filter: blur(5px);
        }
        
        .order-details {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 1.5rem;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            margin-bottom: 2rem;
        }
        
        .order-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1rem;
            padding-bottom: 0.5rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .order-number {
            font-weight: bold;
            font-size: 1.2rem;
        }
        
        .order-status {
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
        }
        
        .status-draft {
            background: rgba(108, 117, 125, 0.2);
            border: 1px solid rgba(108, 117, 125, 0.5);
            color: #6c757d;
        }
        
        .order-items {
            margin-bottom: 1rem;
        }
        
        .order-item {
            display: flex;
            justify-content: space-between;
            padding: 0.75rem 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .item-info {
            flex: 1;
        }
        
        .item-name {
            font-weight: bold;
        }
        
        .item-price {
            opacity: 0.8;
            font-size: 0.9rem;
        }
        
        .item-quantity {
            min-width: 80px;
            text-align: center;
        }
        
        .item-total {
            min-width: 100px;
            text-align: right;
            font-weight: bold;
        }
        
        .order-total {
            font-size: 1.5rem;
            font-weight: bold;
            text-align: right;
            margin: 1rem 0;
            padding-top: 0.5rem;
            border-top: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .action-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            margin-top: 2rem;
        }
        
        .btn {
            padding: 0.75rem 2rem;
            border-radius: 8px;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        
        .btn-primary {
            background: linear-gradient(45deg, #007bff, #0056b3);
            border: none;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 123, 255, 0.4);
        }
        
        .btn-success {
            background: linear-gradient(45deg, #28a745, #20c997);
            border: none;
        }
        
        .btn-success:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(40, 167, 69, 0.4);
        }
        
        .btn-warning {
            background: linear-gradient(45deg, #ffc107, #e0a800);
            border: none;
            color: #212529;
        }
        
        .btn-warning:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 193, 7, 0.4);
        }
        
        .empty-order {
            text-align: center;
            padding: 2rem;
            color: rgba(255, 255, 255, 0.7);
        }
        
        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @media (max-width: 768px) {
            .header {
                flex-direction: column;
                gap: 1rem;
                text-align: center;
            }
            
            .order-item {
                flex-direction: column;
                gap: 0.5rem;
            }
            
            .item-quantity, .item-total {
                text-align: left;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="header-title">
                <i class="fas fa-shopping-cart"></i> Your Order
            </h1>
            <div class="table-info">
                <i class="fas fa-chair"></i> <%= @table.display_name %> | 
                <i class="fas fa-user"></i> <%= session[:customer_name] %>
            </div>
        </div>
        
        <% if @current_order && @order_items.any? %>
            <div class="order-details">
                <div class="order-header">
                    <div class="order-number"><%= @current_order.order_number %></div>
                    <div class="order-status status-draft">
                        Draft
                    </div>
                </div>
                
                <div class="order-items">
                    <% @order_items.each do |item| %>
                        <div class="order-item">
                            <div class="item-info">
                                <div class="item-name"><%= item.menu_item.name %></div>
                                <div class="item-price">LKR <%= item.unit_price %> each</div>
                            </div>
                            <div class="item-quantity">
                                Quantity: <%= item.quantity %>
                            </div>
                            <div class="item-total">
                                LKR <%= item.subtotal %>
                            </div>
                        </div>
                    <% end %>
                </div>
                
                <div class="order-total">
                    Total: LKR <%= @current_order.total_amount %>
                </div>
            </div>
            
            <div class="action-buttons">
                <a href="<%= customer_menu_path %>" class="btn btn-warning">
                    <i class="fas fa-arrow-left"></i> Back to Menu
                </a>
                <button class="btn btn-success" onclick="placeOrder()">
                    <i class="fas fa-paper-plane"></i> Place Order
                </button>
            </div>
        <% else %>
            <div class="order-details">
                <div class="empty-order">
                    <i class="fas fa-shopping-cart fa-3x mb-3"></i>
                    <p>Your order is empty.</p>
                    <p>Add some items from the menu to get started!</p>
                </div>
                
                <div class="action-buttons">
                    <a href="<%= customer_menu_path %>" class="btn btn-primary">
                        <i class="fas fa-utensils"></i> Browse Menu
                    </a>
                </div>
            </div>
        <% end %>
    </div>
    
    <script>
        function placeOrder() {
            if (confirm('Are you sure you want to place this order?')) {
                // Redirect to place order
                window.location.href = '<%= customer_place_order_path %>';
            }
        }
    </script>
</body>
</html>